<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="AppFVC.Views.TermsPage"
             BackgroundColor="#6FCF97">

    <ScrollView>
        <StackLayout>
            <Frame CornerRadius="5" VerticalOptions="Start" Margin="40,55,40,30" Padding="0">
                <StackLayout VerticalOptions="Start" Padding="0,0,13,30" BackgroundColor="White">
                    <ImageButton Source="iconExit50px.png" BackgroundColor="Transparent" Command="{Binding NavigationPop}" HeightRequest="30" HorizontalOptions="Start" Margin="0"></ImageButton>
                    <Label Text="Termos de uso" FontSize="20" VerticalOptions="Start" Padding="25,0,0,0" TextColor="#303030"></Label>
                    <Label Padding="25,10,30,0" TextColor="#737373" FontSize="16" Text="Aguarde... Estamos trabalhando nisso."></Label>
                </StackLayout>
            </Frame>
        </StackLayout>
    </ScrollView>
</ContentPage>