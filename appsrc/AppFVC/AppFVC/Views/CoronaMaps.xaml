<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="AppFVC.Views.CoronaMaps"
             BackgroundColor="#6FCF97">
    
    <Frame 
            CornerRadius="5"
            HasShadow="True" 
            VerticalOptions="CenterAndExpand"
            BackgroundColor="White" 
            Margin="30,30,30,30"
           Padding="5"
            >
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
            </Grid.RowDefinitions>
           
            <Image VerticalOptions="Fill" HorizontalOptions="FillAndExpand" Grid.Row="0" Source="MapaCorona.png"></Image>
            <ImageButton Source="iconExit50px.png" BackgroundColor="Gray" Margin="2,2,0,0" Command="{Binding NavigationPop}" VerticalOptions="Start"  HeightRequest="30" CornerRadius="15"  HorizontalOptions="Start" Grid.Row="0"></ImageButton>

        </Grid>
        
    </Frame>

</ContentPage>